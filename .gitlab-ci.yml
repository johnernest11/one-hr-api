stages:
  - test

phpunit:
  # test
  image: "serversideup/php:8.2-fpm-nginx"
  stage: test
  before_script:
    - php -v
    - sudo install-php-extensions intl
    - echo "Installed intl PHP Extension"
    - echo "$WEBKIT_TESTING_ENV" > ".env.testing"
    - composer install
    - echo "Composer install completed"
  script:
    - php artisan test
