stages:
  - test
  - build
  - deploy

image:
  name: serversideup/php:8:2-fpm-nginx

phpunit:
  stage: test
  before_script:
    - install-php-extensions intl
    - composer install
  script:
    - cp .env.example .env.testing
    - php artisan test
