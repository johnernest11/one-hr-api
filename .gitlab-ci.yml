stages:
  - test

phpunit:
  image: "jegramos/webkit-php:v1.0"
  stage: test
  before_script:
    - touch .env .env.testing
    - cp -f $WEBKIT_TESTING_ENV .env .env.testing
    - cat .env.testing
    - composer install
  script:
    - php artisan test
