stages:
  - test

phpunit:
  image: "jegramos/webkit-php:v1.0"
  stage: test
  before_script:
    - cp $WEBKIT_TESTING_ENV .env
    - cp $WEBKIT_TESTING_ENV .env.testing
    - cat .env.testing
    - composer install
  script:
    - php artisan test
